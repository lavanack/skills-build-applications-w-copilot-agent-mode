# Vite exposes frontend environment variables that use the VITE_ prefix.
# Define this for Codespaces so the frontend can call:
# https://${import.meta.env.VITE_CODESPACE_NAME}-8000.app.github.dev/api/[component]/
VITE_CODESPACE_NAME=your-codespace-name